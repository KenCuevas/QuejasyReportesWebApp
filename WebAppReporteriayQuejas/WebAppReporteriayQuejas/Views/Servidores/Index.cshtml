@model IEnumerable<WebAppReporteriayQuejas.Servidor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Identificacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Identificacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Servidor }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Servidor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Servidor })
        </td>
    </tr>
}

</table>
