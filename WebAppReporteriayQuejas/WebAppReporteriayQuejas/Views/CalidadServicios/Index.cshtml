@model IEnumerable<WebAppReporteriayQuejas.TBCalidadServicio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescServicio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescServicio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_CalidadServicio }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_CalidadServicio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_CalidadServicio })
        </td>
    </tr>
}

</table>
