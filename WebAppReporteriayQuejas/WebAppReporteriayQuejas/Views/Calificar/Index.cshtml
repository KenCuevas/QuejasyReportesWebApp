@model IEnumerable<WebAppReporteriayQuejas.Calidad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescripcionServicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TBCalidadServicio.DescServicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionServicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TBCalidadServicio.DescServicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_tiposervicio }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_tiposervicio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_tiposervicio })
        </td>
    </tr>
}

</table>
